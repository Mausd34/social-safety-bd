# Leave unset to use the Vite dev proxy (recommended - no CORS needed).
# Set an absolute URL only when the API is hosted on a different origin.
# VITE_API_URL=http://127.0.0.1:8000/api

# Where the Vite dev proxy forwards /api requests (defaults to Django on :8000).
# VITE_BACKEND_URL=http://127.0.0.1:8000

